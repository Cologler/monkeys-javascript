# AGENTS.md

- Never edit files under `dist/` directly.
- Add JSDoc to public APIs, including parameter and return types.
- Do not use arrow function expressions; define functions with `function` syntax.
